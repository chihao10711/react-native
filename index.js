/**
 * @format
 */

import {AppRegistry} from 'react-native';
import MainApp from './src/main_app';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => MainApp);
